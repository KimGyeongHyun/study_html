document
  .querySelector(".header .text-box .head1")
  .getAnimations()
  .forEach((animation) => {
    animation.effect.composite = "add";
  });

document
  .querySelector(".header .text-box .head2")
  .getAnimations()
  .forEach((animation) => {
    animation.effect.composite = "add";
  });

document
  .querySelector(".header .text-box .head3")
  .getAnimations()
  .forEach((animation) => {
    animation.effect.composite = "add";
  });
